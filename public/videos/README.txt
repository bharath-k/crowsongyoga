Put your intro video here (for example: intro.mp4).

Then open src/content/site.yaml and set:

  media:
    intro_video: "/videos/intro.mp4"

Until you add a video, the site shows a poster placeholder instead.
Keep videos small (ideally under ~20 MB) so the page loads quickly.
